#include <bits/stdc++.h>
#include <algorithm>
using namespace std;

int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int t;
    cin >> t;
    while (t--)
    {
        long int n;
        cin >> n;
        long int ans = n / 2 + 1;
        cout << ans << endl;
    }
    return 0;
}